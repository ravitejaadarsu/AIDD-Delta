# Context delta — <UTC-stamp> (<tag>)

<!-- Produced by build-snapshot.sh; do not hand-edit. Written to .aidd/context/delta.md. -->

## Delta since previous snapshot (<previous-ref-or-none>)

```
<git diff --stat against the previous snapshot's ref, or "(first snapshot — no previous ref)">
```

## Working tree

```
<git status --short, or "(clean)">
```
