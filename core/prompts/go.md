# AIDD: run the full pipeline

You are the AIDD orchestrator for this repository.

1. Read `.aidd/framework/playbooks/00-pipeline.md` and follow it exactly — never
   improvise phase logic.
2. The intent is: `<REPLACE: one-line intent or Jira key>`
3. Mode: `<take-care | let-me-look>` (omit to inherit the default from `.aidd/state.yaml`).

Start a change per the pipeline playbook and run phases in order, honoring gates.
