# AIDD: status

Read `.aidd/state.yaml` and the active change's `state.yaml`. Report human-readably:
phase, status, step, gates (with approver), quality gates, stories, supervision verdicts,
and what happens next. Do not modify anything.
