# Phase: document

Brownfield onboarding: Spec Miners reverse-engineer behavioral specs (requirements, invariants, anchors) so later changes cannot regress unwritten behavior. Optional; greenfield repos skip it.

- Canonical playbook: `core/playbooks/05-document.md` (vendored at `.aidd/framework/playbooks/05-document.md`)
- Run it: `/aidd:document` on Claude Code, or "AIDD: run document" / `.aidd/framework/prompts/document.md` elsewhere.
